= simple_form_for @tag, html: { class: 'form-horizontal' } do |f|
  = f.input :name, input_html: {value: @tag.name}

  = link_to t('.cancel', default: t("helpers.links.cancel")), users_path, class: 'btn'