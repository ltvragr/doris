- title "Search results for \"#{@search_string}\""

%h4= "Users: " + pluralize(@users.size, 'result') + " found"
%ul
  - @users.each do |user|
    %li= link_to user.first_name + " " + user.last_name, user
%br
%h4= "Labs: " + pluralize(@labs.size, 'result') + " found"

%ul
  - @labs.each do |lab|
    %li= link_to lab.name, lab
%br
%h4= "Projects: " + pluralize(@projects.size, 'result') + " found"
%ul
  - @projects.each do |project|
    %li= link_to project.name, project